syntax = "proto3";

package ai_service;

import "ai_service/requests.proto";
import "ai_service/responses.proto";

// The AI service definition
service AiService {
    rpc Health(HealthRequest) returns (HealthResponse);
    rpc GenerateNotes(NoteGenerationRequest) returns (NoteGenerationResponse);
}
